{
  "name": "tcp-playground-client",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build": "rm -rf dist && tsc -p .",
    "start": "node dist",
    "dev": "yarn build && nodemon --watch './dist' --ext 'ts,tsx' --exec 'yarn start' & tsc -w"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^16.10.1",
    "nodemon": "^2.0.13",
    "typescript": "^4.4.3"
  }
}
